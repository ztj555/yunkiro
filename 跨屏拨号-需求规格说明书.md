# 跨屏拨号 v1 重构需求规格说明书

> 版本：v7.0-draft  
> 日期：2026-05-31  
> 目的：基于 v6 项目实战经验，提炼真实产品需求，允许技术栈自由选型，目标 **更轻量、更低内存、更高连接稳定性**

---

## 一、产品定义

### 1.1 产品是什么

**跨屏一键拨号系统**。让电销/客服人员在自己的电脑上（浏览器或桌面软件）看到一个手机号时，点击一个按钮，号码自动发送到自己的 Android 手机上完成拨号，无需低头找手机、手动输入号码。

### 1.2 典型用户画像

| 角色 | 场景 | 痛点 |
|------|------|------|
| 电销坐席 | 每天拨打 100-300 通电话，电脑上 CRM 系统展示客户列表 | 手动拨号慢，容易拨错，打断工作流 |
| 客服主管 | 管理 5-20 人团队，使用多台手机轮换拨打 | 需要切换手机，希望了解每台手机的拨打状态 |
| 个人自由职业 | 在家办公，一台电脑 + 一台手机 | 希望简单好用、不占资源、稳定不崩 |

### 1.3 核心价值主张

> **在电脑上看到号码 → 点击 → 手机自动拨出。无论手机和电脑在同一 WiFi 还是异地，都能工作。**

---

## 二、用户核心旅程

### 2.1 首次使用（从零到第一次拨号成功）

```
1. 用户在手机安装 APP → 打开 → 看到 4 位配对码
2. 用户在电脑安装软件 → 打开 → 输入配对码
3. 自动完成连接（局域网优先，不可用时云端兜底）
4. 电脑界面显示"已连接，可以拨号"
5. 用户在浏览器选中一个号码 → 弹出"一键拨号"按钮
6. 点击 → 手机弹窗/自动拨出 → 电脑显示"拨号成功"
```

**体验要求**：从安装到第一次拨号成功，不超过 2 分钟，不需要任何网络配置知识。

### 2.2 日常使用

```
1. 早上到公司 → 电脑开机自启 → 手机打开 APP
2. 自动连接（无需任何手动操作）
3. 开始工作 → 在 CRM/网页上点击号码 → 自动拨号
4. 下班 → 关电脑 → 手机 APP 后台保持（第二天自动连）
```

### 2.3 异常场景

```
1. 手机没电关机 → 电脑显示"手机离线"
2. 手机充电开机 → 自动恢复连接
3. 公司 WiFi 断了 → 自动切到手机蜂窝网络（云端中继）
4. WiFi 恢复了 → 自动切回局域网（更快）
5. 手机息屏 2 小时 → 亮屏后 3 秒内恢复可用
```

---

## 三、功能需求

### 3.1 电脑端（桌面软件）

#### F-D-01 自动发现与连接
- 启动后自动通过局域网发现手机（无需手动输入 IP）
- 若局域网不可达，自动通过云端中继连接
- 连接状态实时显示：LAN / 云端 / 离线
- 支持手动触发重连

#### F-D-02 号码输入与拨号
- 支持手动输入号码（键盘输入 + 屏幕拨号盘）
- 自动检测剪贴板中的手机号（无需粘贴）
- 点击"拨号"按钮 → 发送指令到手机
- 拨号结果反馈：成功 / 失败 / 超时

#### F-D-03 挂断
- 电脑端一键挂断当前通话
- 显示通话状态

#### F-D-04 发送短信
- 支持通过手机发送短信
- 支持短信模板（预设常用话术）
- 支持自定义短信内容

#### F-D-05 多手机管理
- 支持同时在线最多 **5 台**手机（v6 为 2 台，扩展）
- 每台手机独立显示：名称、连接方式、信号强度标识
- 点击切换活跃手机
- 支持为每台手机设置备注名（如"工作机A"、"备用机"）

#### F-D-06 系统集成
- 支持开机自启动
- 支持最小化到系统托盘
- 支持窗口置顶
- 占用内存：**空闲 < 80MB，活跃 < 150MB**（v6 约 200-300MB）

#### F-D-07 界面
- 支持亮色/暗色主题
- 界面简洁，核心操作（拨号）不超过 2 次点击
- 窗口尺寸可调，支持紧凑模式（仅显示号码框 + 拨号按钮）

### 3.2 浏览器扩展

#### F-E-01 号码识别
- 自动识别网页中的中国大陆手机号（11 位 1 开头）
- 在号码旁或选中号码时显示"一键拨号"按钮
- 支持在特定 CRM 页面做深度适配（如识别表格中的号码列）

#### F-E-02 通信
- 通过本地 HTTP/WebSocket 与桌面软件通信
- 发送拨号指令
- 接收拨号结果并展示

#### F-E-03 轻量化
- 扩展体积 < 500KB
- 内存占用 < 30MB
- 不使用持续后台 Service Worker 轮询（事件驱动）

### 3.3 手机端（Android）

#### F-M-01 连接管理
- 支持局域网直连（WebSocket 或更优协议）
- 支持云端中继连接
- 自动检测并切换最优通道：LAN 优先，Cloud 兜底
- 网络变化时（WiFi↔蜂窝）自动重新协商连接

#### F-M-02 拨号执行
- 收到拨号指令后自动调起系统拨号界面
- 支持双卡选择（卡1 / 卡2 / 每次询问 / 记忆上次选择）
- 拨号结果回报给电脑端
- 通话结束后自动更新状态

#### F-M-03 后台保活
- 前台服务常驻通知栏（Android 规范要求）
- 支持开机自启动
- Doze/省电模式下的连接保持策略
- 被系统杀死后自动重启

#### F-M-04 资源占用
- APK 体积 < 8MB（v6 约 11.8MB）
- 空闲内存 < 60MB
- CPU 使用率 < 1%（空闲时）
- 后台流量 < 5MB/天（仅心跳 + 指令）

#### F-M-05 唤醒机制
- 支持局域网 UDP 唤醒
- 支持云端推送唤醒（FCM/厂商推送通道或 relay 长连接）
- 亮屏后 3 秒内完成连接健康检查并恢复可用

### 3.4 云端中继

#### F-R-01 消息转发
- 按配对码（PIN）分组管理客户端
- PC 和手机通过 relay 双向转发消息
- 支持精确路由（同一 PIN 下多个手机时，发给指定设备）
- 转发延迟 < 200ms（P99）

#### F-R-02 状态同步
- 手机上线时 → 立即通知同组所有 PC
- 手机离线时 → 立即通知同组所有 PC（**v6 缺失的能力**）
- PC 上线时 → 补发已在线手机列表
- 主动心跳检测，及时清理僵尸连接

#### F-R-03 部署
- 单文件部署，无外部依赖（或仅依赖语言运行时）
- 支持 Docker 部署
- 支持配置端口和日志级别
- 内存占用 < 50MB（100 并发连接）

#### F-R-04 安全
- 仅转发已配对（PIN 匹配）的消息
- 不存储任何消息内容
- 支持 TLS/WSS 加密传输

### 3.5 可选增强功能（v7.1+）

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 通话记录同步 | 手机通话记录回传电脑端，支持导出 | P2 |
| 统计分析 | 今日/本周拨打次数、通时统计 | P2 |
| 语音通话 | 电脑端麦克风 → 手机端通话（类似蓝牙耳机） | P3 |
| iOS 支持 | iPhone 端 APP | P3 |
| 团队管理 | 主管查看团队拨打数据 | P3 |

---

## 四、非功能需求（重点）

### 4.1 性能

| 指标 | 目标值 | 测量方法 |
|------|--------|---------|
| 端到端拨号延迟（LAN） | < 500ms | 电脑点击到手机弹出拨号界面 |
| 端到端拨号延迟（Cloud） | < 2s | 同上 |
| 连接恢复时间（WiFi 切换） | < 10s | WiFi 断到重新可用 |
| 连接恢复时间（亮屏） | < 3s | 亮屏到拨号可用 |
| 电脑端内存（空闲） | < 80MB | 任务管理器 |
| 电脑端内存（活跃） | < 150MB | 同上 |
| 手机端内存（后台） | < 60MB | adb shell dumpsys meminfo |
| 手机端 APK 体积 | < 8MB | APK 文件大小 |
| 电脑端安装包 | < 50MB | 安装包大小 |
| relay 内存（100 连接） | < 50MB | 服务器监控 |

### 4.2 稳定性

| 指标 | 目标值 |
|------|--------|
| 7×24 小时连续运行无崩溃 | 必须 |
| 网络切换（WiFi↔蜂窝）成功率 | > 99% |
| 断线自动重连成功率 | > 99.5% |
| 消息送达率（ACK 确认） | > 99.9% |
| relay 单点故障恢复时间 | < 30s（客户端自动切换备用 relay） |

### 4.3 连接稳定性（核心难点）

这是整个项目最关键的挑战。需要在以下环境中保证连接可靠：

| 环境 | 挑战 | 解决方案方向 |
|------|------|-------------|
| 同一 WiFi | 最简单，TCP/WS 直连 | mDNS 或 UDP 广播发现 + 直连 |
| 手机切蜂窝 | IP 全变，需要 relay | 云端中继自动降级 |
| 手机 Doze 模式 | WebSocket 可能变僵尸 | 亮屏快速健康检查 + 主动重建 |
| 企业防火墙 | 可能封非标准端口 | 支持 443 端口 WSS |
| NAT 穿透 | 部分网络无法直连 | relay 作为可靠兜底 |
| WiFi 信号弱 | 丢包率高 | 自动切换 relay，不依赖弱 WiFi |
| 多网卡 PC | IP 选择问题 | 绑定所有网卡 + 手机端多 IP 尝试 |
| 手机热点 | PC 连手机热点 | 应识别为 LAN，不走 relay |

### 4.4 安全

- 配对码机制：4 位数字，仅需在局域网或 relay 内可猜（非安全敏感场景，但需防误连）
- 通信加密：局域网可明文（信任内网），云端必须 WSS/TLS
- 不传输任何用户个人信息到 relay（仅转发 PIN + 指令 + 设备名）
- 手机号不存储在 relay 端

---

## 五、技术选型指引（非强制，供参考）

### 5.1 选型原则

1. **优先选择编译型语言**（更小内存、更快启动）
2. **优先选择单二进制部署**（降低用户安装门槛）
3. **优先选择内置网络库丰富的生态**（减少依赖）
4. **允许不同端使用不同语言**（取各自平台最优解）

### 5.2 建议选型方向

| 组件 | 推荐方向 | 理由 |
|------|---------|------|
| 电脑端 | Rust (Tauri) 或 Go (Wails) | 内存小（Tauri < 20MB 基准），二进制分发，自带 WebView |
| 手机端 | Kotlin (原生) 或 Flutter | Android 原生最稳定；Flutter 可复用代码且为未来 iOS 铺路 |
| 浏览器扩展 | 纯 JS/TS (MV3) | Chrome 限定，无替代方案。但应尽量轻量 |
| 云端 relay | Go 或 Rust | 编译型、高并发、低内存、单二进制部署 |
| 通信协议 | WebSocket + 自定义 JSON | 成熟稳定；可选 WebRTC 做 LAN 直连（NAT 穿透更强） |

### 5.3 推荐但非强制的协议栈

```
┌──────────────────────────────────────────────────┐
│                  通信层                           │
│  发现：mDNS (LAN) / 配置直连 (Cloud)             │
│  传输：WebSocket (可靠) + UDP (唤醒)             │
│  备选：WebRTC DataChannel (P2P，NAT穿透更优)     │
│  序列化：JSON (兼容性) 或 MessagePack (体积)     │
├──────────────────────────────────────────────────┤
│                  应用层协议                       │
│  认证：PIN 码，一次性握手                         │
│  保活：30s 双向 ping/pong                        │
│  指令：dial / hangup / sms（带 messageId + ACK） │
│  状态：phone_hello / phone_left / device_status  │
│  结果：dial_result / sms_result                  │
└──────────────────────────────────────────────────┘
```

### 5.4 关键架构决策（由实现者定夺）

| 决策点 | 选项 A | 选项 B | 建议 |
|--------|--------|--------|------|
| PC 端框架 | Tauri (Rust) | Electron (JS) | **Tauri**，内存和安装包优势巨大 |
| 手机端 | Kotlin 原生 | Flutter | **Kotlin**，最稳定的 Android 体验 |
| relay 语言 | Go | Rust | **Go**，goroutine 天然适合并发连接 |
| LAN 传输 | WebSocket | WebRTC | **WebSocket** 优先，简单可靠 |
| 序列化 | JSON | MessagePack | **JSON**，调试友好，Gzip 后差异不大 |
| 心跳机制 | 应用层 ping/pong | 协议层 keep-alive | **应用层**，穿透代理更可靠 |

---

## 六、架构设计要求

### 6.1 连接状态机（必须精确实现）

每个客户端（PC 或手机）对每个连接通道维护独立状态机：

```
                  ┌─────────┐
                  │DISCONNECTED│
                  └────┬────┘
                       │ connect()
                  ┌────▼────┐
                  │DISCOVERING│ (LAN only)
                  └────┬────┘
                  ┌────▼────┐
                  │CONNECTING│
                  └────┬────┘
                  ┌────▼────┐
                  │CONNECTED │
                  └────┬────┘
         ┌────────────┼────────────┐
    ping_timeout  channel_dead  manual_disconnect
         │            │              │
    ┌────▼────┐  ┌────▼────┐   ┌────▼────────┐
    │RECONNECT│  │DEGRADE  │   │DISCONNECTED │
    │(阶梯延迟)│  │(切换通道)│   │(不自动重连) │
    └─────────┘  └─────────┘   └─────────────┘
```

### 6.2 双通道管理原则

1. **LAN 绝对优先**：只要 LAN 可用，始终优先使用
2. **Cloud 热备份**：LAN 在线时 Cloud 保持连接但不承载业务流量
3. **切换零感知**：用户不需要知道当前走的是 LAN 还是 Cloud
4. **消息幂等**：双通道可能同时尝试送达同一消息，手机端需去重
5. **通道健康独立检测**：每个通道独立心跳，超时只影响该通道

### 6.3 消息可靠性保证

```
发送端                            接收端
  │                                │
  │── msg(messageId, type, body)──▶│
  │                                │── 执行业务逻辑
  │◀── ack(messageId) ────────────│
  │                                │
  │  [超时 3s 未收到 ACK]          │
  │── 重试备选通道 ───────────────▶│
  │                                │── 去重检查(messageId)
  │◀── ack(messageId) ────────────│
  │                                │
  │  [若收到业务结果比 ACK 早]      │
  │  dial_result/sms_result 也视为 │
  │  成功确认（ACK 兜底机制）       │
```

### 6.4 relay 设计原则

1. **纯转发，不存储业务数据**
2. **必须主动推送状态变更**（手机上线/离线 → 通知 PC）
3. **同设备重连时清理旧连接**
4. **健康检查**：45 秒无消息 → 主动断开
5. **支持多实例部署**（客户端可配置多个 relay 地址，自动故障转移）

---

## 七、测试验收标准

### 7.1 必须通过的场景

以下 12 个场景都必须通过自动化或手动验证：

| # | 场景 | 验收标准 |
|---|------|---------|
| 1 | 手机退出重开 | 10 秒内 PC 恢复显示在线，可正常拨号 |
| 2 | 手动断开连接 | 手机端不自动重连 |
| 3 | 手机息屏 >30 分钟后亮屏 | 3 秒内拨号可用 |
| 4 | 每天首次启动 | 自动连接，无需手动操作 |
| 5 | WiFi 切蜂窝再切回 | 全程可拨号，切换时无感知 |
| 6 | 云端 relay 掉线恢复 | 2 分钟内自动恢复 |
| 7 | 电脑端重启 | 自动恢复连接 |
| 8 | 手机重启 | 开机后自动重连 |
| 9 | 多手机切换 | 每次拨号到正确手机 |
| 10 | 拨号时手机不在线 | 自动唤醒 → 补发拨号，30s 内完成 |
| 11 | 4-8 小时连续运行 | 连接不断，心跳正常，内存不涨 |
| 12 | 网络频繁抖动 | 阶梯降频，最终恢复，不崩溃 |

### 7.2 压力测试

| 指标 | 目标 |
|------|------|
| relay 并发连接数 | 500（单实例） |
| relay 消息吞吐量 | 1000 msg/s |
| PC 端同时连接手机数 | 5 |
| 连续运行时间 | 7×24h 无内存泄漏 |

---

## 八、项目交付物

### 8.1 必须交付

| 交付物 | 说明 |
|--------|------|
| 手机端 APP | Android APK，支持 Android 8+ |
| 电脑端软件 | Windows 安装包（macOS 可选） |
| 浏览器扩展 | Chrome/Edge MV3 扩展包 |
| 云端 relay | 可独立部署的服务端程序 |
| 使用说明 | 面向用户的操作指南 |

### 8.2 建议交付

| 交付物 | 说明 |
|--------|------|
| 开发者文档 | 通信协议、API 文档、架构说明 |
| Docker 镜像 | relay 的 Docker 部署方案 |
| CI/CD 流水线 | GitHub Actions 自动构建 |

---

## 九、明确不要的东西

以下是 v6 中存在但 **v7 不需要**的东西：

- ❌ 花哨的拨号动画（烟花/弹跳特效）
- ❌ 16 套主题系统（保留 2 套即可：亮/暗）
- ❌ 财运/吉利话等娱乐元素
- ❌ 内置 Web 管理仪表盘（可独立为可选组件）
- ❌ C#/C++ 启动器（语言自带编译能力即可）
- ❌ Python 版 relay（统一用一种语言维护）
- ❌ 复杂的手动配置项（能做自动的就不要手动）

---

## 十、FAQ（给实现者的额外说明）

**Q: 为什么不用 WebRTC？**
A: 可以用。WebRTC 的 NAT 穿透能力比 UDP 广播更强，但复杂度也更高。如果选 WebRTC，需要额外部署 STUN/TURN 服务器。建议先用 WebSocket + relay 快速验证，后期可选升级为 WebRTC。

**Q: PIN 码安全吗？**
A: 4 位数字仅用于局域网或 relay 内部区分不同用户，不承载身份认证。安全依赖于局域网隔离（LAN 模式）和 relay 的 PIN 分组隔离（Cloud 模式）。如需更高安全性可支持自定义长 PIN。

**Q: 为什么要支持 5 台手机？**
A: v6 支持 2 台，但电销团队常有备用机。扩展到 5 台覆盖多卡多机场景。架构设计上应该支持 N 台（N 仅受内存/带宽限制）。

**Q: 手机端必须 Android 吗？**
A: v7 必须支持 Android。iOS 因系统限制（无法后台长连接、无法自动拨号）暂不作为必须项，但架构上预留扩展可能。

---

*本文档由 AutoDial v6 实战经验提炼而成，不等于 v6 的功能清单。实现者可基于此文档和自身的语言/框架偏好做出技术决策。*
